# AI Agent 全栈解决方案

这是一个功能强大的 AI 智能体对话体系统，**基于 Coze (扣子) 平台能力封装**， Laravel 的后端 API 服务和 UniApp 的微信小程序前端。项目集成了 AI 对话、工作流，用户管理、支付系统，分销，卡密等以及完善的后台管理功能。

## 🚀 主要功能

### 📱 前端 (微信小程序)
- **AI 智能对话**: 支持与 AI Agent 进行实时对话，流式响应。
- **工作流**: 支持异步工作流。
- **富文本渲染**: 完美支持 Markdown 格式，包含代码高亮、表格、公式等渲染。
- **应用广场**: 浏览和使用各种预设的 AI 应用/智能体。
- **用户中心**:
  - 账号登录/注册
  - VIP 会员系统
  - 积分/算力管理
  - 充值与订单记录
- **交互体验**: 现代化的 UI 设计，支持深色模式适配（依赖系统设置或 Tailwind 配置）。

### 🖥️ 后端 (API & 管理后台)
- **RESTful API**: 为小程序提供全套数据接口。
- **后台管理系统**: 基于 Owl Admin (Amis) 构建的高效管理后台。
  - 用户管理：查看和管理用户信息。
  - 订单管理：监控充值和消费订单。
  - AI 配置：管理 Agent、工作流、模型参数等。
  - 系统设置：配置小程序参数、支付参数等。
- **微信集成**: 集成 EasyWeChat，支持微信登录、微信支付。
- **安全认证**: 使用 Laravel Sanctum 进行 API 认证。

## 📂 目录结构

- `api/`: **后端项目** (Laravel 12 + Owl Admin)
- `app/`: **前端项目** (UniApp + Vue3 + TypeScript + TailwindCSS)

## 🛠️ 安装指南

### 前置要求
- **PHP**: >= 8.2
- **Node.js**: >= 18.0
- **Composer**: PHP 包管理器
- **MySQL**: 5.7 或 8.0

### 1. 后端安装 (API)

进入 `api` 目录并安装依赖：

```bash
cd api

# 安装 PHP 依赖
composer install


# 数据库设置 (二选一)

**方案 A: 全新安装**

```bash
# 运行数据库迁移
php artisan migrate

# 初始化后台管理数据
php artisan admin:install
``

```bash
php artisan serve
```

后台管理地址通常为: `http://127.0.0.1:8000/admin`

### 2. 前端安装 (小程序)

进入 `app` 目录并安装依赖：

```bash
cd app

# 安装 Node.js 依赖
npm install
# 或者使用 pnpm (推荐)
pnpm install
```

配置后端地址：
检查 `app/src` 下的配置文件 (如 `env.d.ts`, `.env` 或 API 配置文件)，确保 API 请求地址指向你的后端服务地址 (例如 `http://127.0.0.1:8000/api`)。

运行开发环境：

```bash
# 编译并监听文件修改 (微信小程序)
npm run dev:mp-weixin
```

**导入微信开发者工具**:
1. 打开微信开发者工具。
2. 选择"导入项目"。
3. 目录选择: `app/dist/dev/mp-weixin`。
4. AppID: 使用你自己的小程序 AppID (需在 `app/manifest.json` 中配置)。

构建生产版本：

```bash
npm run build:mp-weixin
```

## � 数据库更新指南

当代码更新涉及数据库结构变更时（如新增字段、表）：

1. 拉取最新代码。
2. 在 `api` 目录下运行迁移命令：
   ```bash
   php artisan migrate
   ```
   *注意：该命令会自动检测差异并应用变更，不会丢失现有数据。*

## 🛠️ 初始化数据 (Seeding)

如果您想将现有数据库中的数据（如配置项、初始用户）作为项目的初始数据：

1. **生成 Seeder**: 使用 `iseed` 工具将现有数据导出为 Seeder 文件。
   ```bash
   # 导出指定表的数据 (例如 admin_users, settings)
   php artisan iseed admin_users,settings
   ```

2. **应用 Seeder**: 新环境安装时，运行以下命令填充数据。
   ```bash
   php artisan db:seed
   ```

## �📝 技术栈

- **后端**: Laravel 12, MySQL, Owl Admin (Amis), EasyWeChat, Docker
- **前端**: UniApp, Vue 3, TypeScript, Tailwind CSS, Pinia
# cozeminiapp
